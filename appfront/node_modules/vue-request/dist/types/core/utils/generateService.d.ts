import type { IService } from './types';
declare const generateService: <R, P extends unknown[]>(service: IService<R, P>) => (() => Promise<R>) | ((...args: P) => Promise<R>);
export default generateService;
